# Auto-ABM 核心需求

## 1. 产品定位

**Auto-ABM = 以「仿真（Simulation）」为一等对象的 ABM 科研工作台。**

- 用户操作**研究课题、仿真方案、运行、实验、Trace、ODD 文档**，而非代码仓库。
- 对标：**增强版 NetLogo**（调参、空间可视化、实时曲线）+ **大模型驱动的可溯源解释** + **可复现实验**。
- AI 降低建模与实验的工程摩擦；研究模式下关键操作可人工确认，探索模式支持无人值守自动推进。

---

## 2. 核心对象

```text
Project（研究课题）
  ├─ research_question / target_phenomenon   # 课题表述或目标现象（可选）
  ├─ Simulations[]
  │    ├─ Model（机制与结构，版本化）
  │    ├─ ODD（与 Model 绑定的标准模型说明，七部分，随 Model 版本同步）
  │    ├─ Interface 状态（参数、种子、步数）
  │    ├─ Runs[]（→ Trace + 指标）
  │    └─ Experiments[]（扫描、干预、对比）
  ├─ Conversations[]（对话线程）
  └─ Artifacts[]（图表、报告、复现包等）
```

### 对象说明

| 对象 | 含义 | 要点 |
|---|---|---|
| **Project** | 研究课题容器 | 承载问题陈述、多个 Simulation、共享对话与产物 |
| **Simulation** | 仿真方案 | 稳定身份；含 Model、ODD、运行与实验历史；可 `@` 引用 |
| **Model** | 可执行模型 | Agent / 机制 / 参数 / 环境 / 观测指标；改结构递增版本 |
| **ODD** | 模型标准说明 | 由 Model **自动派生**七部分草稿；可编辑；与 Model 版本一一对应；解释与论文输出的语义层 |
| **Run** | 单次仿真 | 绑定 seed、参数快照、Model 版本；产出 Trace |
| **Trace** | 仿真轨迹 | tick 指标、事件、机制触发；**可解释性的数据根** |
| **Experiment** | 批量实验 | 归属 Simulation；扫描/干预/对比；展开为多个 Run |

### ODD 与 Simulation 的关系

- 每个 Simulation 有一份 **活 ODD**：Purpose、Entities、Process、Design Concepts、Initialization、Input、Submodels。
- Model 变更 → ODD **自动增量更新**对应章节（保留用户手写段落，冲突处标出待复核）。
- ODD 章节与 Model 字段、机制图节点**双向可跳转**；AI 解释机制时优先引用 ODD + Trace，而非自由发挥。
- 导出论文材料时，ODD 为方法与模型描述的主干。

### Simulation 生命周期

```text
需求输入 → 系统生成一批 Simulation 草案
         → 用户选择 / 探索模式自动试跑筛选
         → 正式 Simulation（可迭代改 Model/参数，版本可追溯）
         → fork 为新 Simulation（机制竞争、方案分叉）
```

---

## 3. 交互模型

### 3.1 主界面

```text
┌──────────────┬─────────────────────────────┬──────────────────┐
│  项目导航     │  对话区（可多会话）            │  仿真工作区       │
│  项目/会话    │  提需求 · @仿真 · 看解释     │  Interface       │
│  Simulation  │                             │  画布             │
│  列表        │                             │  Trace / 结果     │
│              │                             │  ODD / 机制       │
└──────────────┴─────────────────────────────┴──────────────────┘
```

- 对话是意图入口；**结论与证据落在 Simulation / Run / Trace / ODD**，可点击溯源。
- MVP 优先打磨**单个 Simulation** 的完整工作区；多 Simulation 以列表/Tab 切换。

### 3.2 @ 引用

`@Simulation` · `@Run` · `@Experiment` · Trace 已刷选区间

示例：`@仿真A 扫描干预强度` · `解释 tick 30–50` · `@仿真A 更新 ODD 实验设置章节`

### 3.3 仿真工作区

| 区块 | 能力 |
|---|---|
| **Interface** | 滑块、Run/Reset、种子步数、实时曲线 |
| **画布** | 网络/网格空间态（运行同步或回放，大 N 降采样） |
| **Trace** | 擦洗时间轴、事件与机制触发；刷选区间 → 触发解释 |
| **机制 / ODD** | 机制结构与 ODD 对照阅读；点机制看 Trace 触发记录 |
| **结果** | Run 与实验的图表、摘要 |

### 3.4 方案生成

用户提出需求后，系统给出 **一批 Simulation 草案**（通常 5–10 个，覆盖不同机制假设、环境设定或建模边界），每个草案附：

- 一句话机制摘要  
- 关键参数与预期宏观行为  
- ODD Purpose / Process 摘要  
- 可选：低步数试跑结果（探索模式自动执行）

用户可逐个查看、对比、采纳；未采纳的存为草稿或丢弃。

---

## 4. 三种使用模式

| 模式 | 用途 |
|---|---|
| **研究模式**（默认） | 人机协作：选方案、调参、跑实验、读解释、改 ODD、导出 |
| **对话模式** | 只读：问机制、问结果、问 ODD 段落含义；不产生变更 |
| **探索模式** | **无人值守**：投入选题或现象 → 自动生成多方案 → 自动试跑与对比 → 排序与报告；用户事后审阅 |

探索模式覆盖 **现象→机制** 与 **机制→现象**；全程结论须映射真实 Run/Trace/指标，禁止编造吻合度或机制证据。

研究模式下：改 Model 结构、大规模批量、覆盖结果、导出复现包等须确认。

---

## 5. 核心功能

### 5.1 建模与 ODD

- 自然语言 → 一批 Simulation 草案 + 各草案 ODD 摘要。  
- 采纳后生成可运行 Model，并生成完整 ODD 草稿。  
- `@仿真` 改机制/参数/初始条件；ODD 相关章节同步更新。  

### 5.2 运行与可视化

- 确定性 Run；完整 Trace；Interface 曲线 + 画布。  
- Run 绑定 Model 版本与参数快照，支持复现。  

### 5.3 可解释性（产品亮点）

大模型 + Trace 结构化数据，提供 **证据链式** 解释，而非泛泛叙述：

| 能力 | 说明 |
|---|---|
| **区间叙事** | 刷选 Trace 区间 → 生成「发生了什么」：指标变化、关键事件、机制触发时序 |
| **机制归因** | 回答「为何此时加速/逆转」→ 引用 `mechanism_fired`、Agent 状态变化、参数取值 |
| **微观—宏观链** | 从个体行为累积解释宏观曲线拐点；可联动画布高亮相关 Agent/边 |
| **反事实追问** | 改参数/干预时点 → 确定性重跑分支 → 双轨迹对照解释差异来源 |
| **异常标注** | 自动标出偏离典型路径的 tick/指标突变，并链到 Trace 事件 |
| **证据卡片** | 每条解释附可点击证据（tick、指标值、事件名、机制 id）；无证据则标「推测」 |
| **ODD 对齐解释** | 解释用语与 ODD Submodels/Process 一致，便于直接写入论文 |

解释请求入口：Trace 刷选、曲线拐点、画布点选、对话 `@Run`、ODD 段落旁「用本次运行解释」。

### 5.4 实验

- `@Simulation`：单参扫描、网格、干预对比、多种子重复。  
- 结果归属 Simulation；对话给摘要，详情在实验与图表对象。  
- 多 Simulation 横向对比同一指标。  

### 5.5 探索模式（无人值守）

- 输入课题或目标现象 → 自动生成多 Simulation 草案 → 自动试跑 → 按吻合度/目标排序 → 输出对比报告与搜索过程树。  
- 可自动 fork、剪枝低分分支、推荐最优方案；用户无需逐步确认。  
- 报告中的数值、排序、机制取舍均来自真实 Run，可追溯 `run_id`。  

### 5.6 输出

- CSV/JSON、图表、实验报告、**复现包**（含 Model、ODD、实验、种子、结果）。  
- ODD 导出 Markdown，作为模型与论文方法部分主干。  

### 5.7 对话

- 每 Project 多条 Conversation；共享下属 Simulation。  
- 默认绑定当前激活 Simulation；`@` 显式指定对象。  

---

## 6. 非功能需求

| 原则 | 要求 |
|---|---|
| **可复现** | 种子、参数、Model 版本全记录；复现包自包含 |
| **可解释** | 解释必有 Trace/指标证据链；无证据标「推测」 |
| **真实性** | 探索模式亦禁止伪造分数、机制或运行结果 |
| **轻量体验** | 仿真工作区为中心；代码与文件按需展开 |

---

## 7. 范围

**MVP**

1. 单 Simulation 工作区（Interface + 画布 + Trace + ODD 只读/基础编辑）  
2. 一批草案生成 → 择一采纳  
3. **区间解释 + 机制归因**（可解释性最小闭环）  
4. `@Simulation` 单参扫描  
5. 研究模式 + 对话模式  
6. 复现包导出（含 ODD）  

**后续**

- 探索模式无人值守全流程  
- 反事实、异常标注、证据卡片增强  
- 多 Simulation 对比、搜索树  
- ODD 与实验章节联动自动撰写  

**不做**

- 以代码/终端为中心的 IDE 形态  
- 数据驱动校准、分布式仿真、3D/GIS  
- Word/LaTeX 导出生态  

---

## 8. 成功标准

用户无需写 Mesa，在一个 Project 内完成：

1. 从问题得到可运行 Simulation 与 ODD  
2. 在 Interface/画布上交互并理解结果  
3. 通过 Trace 区间获得**带证据链**的机制解释  
4. 完成至少一次参数或干预实验  
5. 导出可校验复现包  

体验上是在操作 **ABM 科研仪器**，而非通用编程 Agent。
